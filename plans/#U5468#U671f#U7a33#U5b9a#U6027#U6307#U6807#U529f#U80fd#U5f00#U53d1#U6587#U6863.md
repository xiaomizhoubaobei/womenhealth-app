# 周期稳定性指标功能开发文档

## 文档信息
- **文档版本**: v1.0
- **创建日期**: 2026-02-14
- **文档类型**: 功能开发文档
- **所属模块**: 数据分析与可视化 → 智能预测引擎
- **关联文档**: CYCLE_REGULARITY_SCORING_SPEC.md, TECHNICAL_SPEC.md

---

## 目录
1. [功能概述](#功能概述)
2. [业务需求](#业务需求)
3. [技术设计](#技术设计)
4. [数据模型](#数据模型)
5. [算法实现](#算法实现)
6. [界面设计](#界面设计)
7. [开发计划](#开发计划)
8. [测试方案](#测试方案)
9. [风险提示](#风险提示)

---

## 功能概述

### 功能定义
周期稳定性指标功能是女性健康管理App的核心分析功能之一，通过统计学方法对用户的月经周期数据进行多维度分析，生成稳定性评分和相关指标，帮助用户了解自身周期规律性，及时发现潜在健康问题。

### 核心价值
- **健康监测**: 通过量化指标直观展示周期稳定性
- **早期预警**: 识别周期异常，提示潜在健康风险
- **趋势追踪**: 长期监控周期稳定性变化趋势
- **个性化建议**: 基于评分结果提供针对性健康指导

### 适用场景
1. **日常查看**: 用户在健康统计页面查看周期稳定性报告
2. **自动更新**: 每次记录新周期后自动更新评分
3. **异常提醒**: 评分异常时触发健康提醒通知
4. **报告导出**: 导出健康报告时包含稳定性分析

### 使用频率
- **自动计算**: 每次记录新周期后
- **手动刷新**: 用户主动查看时
- **定期分析**: 每月生成月度报告

---

## 业务需求

### 功能需求

#### 1. 评分计算
- **输入**: 历史周期数据（至少3个完整周期）
- **输出**: 综合评分（0-100分）+ 评分等级 + 各维度得分
- **维度**:
  - 周期一致性（权重40%）
  - 周期正常性（权重30%）
  - 经期稳定性（权重20%）
  - 数据充足性（权重10%）

#### 2. 评分等级
| 等级 | 分数范围 | 显示名称 | 颜色 | 描述 |
|------|---------|---------|------|------|
| 数据不足 | 0 | 数据不足 | #999999 | 需要至少3个周期数据 |
| 非常规律 | 90-100 | 非常规律 | #4CAF50 | 周期非常规律，健康状况良好 |
| 规律 | 80-89 | 规律 | #8BC34A | 周期规律，健康状况良好 |
| 轻微不规律 | 70-79 | 轻微不规律 | #FFC107 | 周期轻微不规律，建议关注 |
| 不规律 | 60-69 | 不规律 | #FF9800 | 周期不规律，建议就医检查 |
| 非常不规律 | 0-59 | 非常不规律 | #F44336 | 周期非常不规律，强烈建议就医 |

#### 3. 统计指标
- 平均周期长度
- 周期标准差
- 变异系数
- 平均经期天数
- 经期标准差
- 异常周期数量
- 周期趋势（稳定/改善/恶化）

#### 4. 健康建议
根据评分等级和各维度得分，生成个性化健康建议：
- **优秀级**: 鼓励保持健康生活方式
- **良好级**: 提供维护建议
- **一般级**: 提供改善建议
- **较差级**: 建议就医检查
- **极差级**: 强烈建议就医

#### 5. 历史趋势
- 展示过去12个月的评分变化趋势
- 使用折线图可视化
- 标注关键变化点

### 非功能需求

#### 1. 性能要求
- 计算响应时间: < 500ms
- 数据加载时间: < 1s
- 图表渲染时间: < 1s

#### 2. 数据要求
- 最少3个周期数据
- 推荐6-12个周期数据
- 最多使用最近12个周期

#### 3. 隐私要求
- 数据本地加密存储
- 不上传云端
- 用户完全控制数据

#### 4. 兼容性要求
- Android 8.0+ (API 26+)
- 支持深色模式
- 支持多语言（中/英）

---

## 技术设计

### 架构设计

采用MVVM + Clean Architecture架构：

```
┌─────────────────────────────────────────┐
│           Presentation Layer             │
│  ┌─────────────┐  ┌─────────────┐       │
│  │   UI Screen │◄─┤  ViewModel  │       │
│  │  (Compose)  │  │  (Hilt)     │       │
│  └─────────────┘  └─────────────┘       │
└─────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────┐
│            Domain Layer                  │
│  ┌─────────────┐  ┌─────────────┐       │
│  │  Use Cases  │  │  Calculator │       │
│  │ (Business)  │  │  (Algorithm)│       │
│  └─────────────┘  └─────────────┘       │
└─────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────┐
│             Data Layer                   │
│  ┌─────────────┐  ┌─────────────┐       │
│  │ Repository  │  │     DAO     │       │
│  │             │◄─┤   (Room)    │       │
│  └─────────────┘  └─────────────┘       │
└─────────────────────────────────────────┘
```

### 技术栈

| 层级 | 技术选型 | 用途 |
|------|---------|------|
| UI层 | Jetpack Compose | 现代化UI框架 |
| UI库 | Material 3 | Material Design 3组件 |
| 图表 | Vico Chart | 数据可视化 |
| 数据层 | Room + SQLCipher | 本地加密数据库 |
| 依赖注入 | Hilt | 依赖管理 |
| 异步处理 | Kotlin Coroutines | 异步任务 |
| 数据流 | Flow | 响应式数据流 |

### 模块划分

```
周期稳定性指标模块
├── domain/
│   ├── model/
│   │   ├── RegularityScore.kt
│   │   ├── ScoreLevel.kt
│   │   └── CycleStatistics.kt
│   ├── usecase/
│   │   ├── CalculateRegularityScoreUseCase.kt
│   │   └── GetScoreTrendUseCase.kt
│   └── calculator/
│       └── RegularityScoreCalculator.kt
├── data/
│   ├── repository/
│   │   └── RegularityScoreRepository.kt
│   └── dao/
│       └── MenstrualCycleDao.kt
└── presentation/
    ├── ui/
    │   ├── score/
    │   │   ├── ScoreScreen.kt
    │   │   ├── ScoreViewModel.kt
    │   │   └── components/
    │   │       ├── ScoreCircleView.kt
    │   │       ├── DimensionProgressView.kt
    │   │       └── TrendChartView.kt
    │   └── health/
    │       └── HealthFragment.kt
    └── navigation/
        └── HealthNavigation.kt
```

---

## 数据模型

### 核心数据模型

#### 1. 评分结果 (RegularityScore)
```kotlin
/**
 * 周期规律性评分结果
 */
data class RegularityScore(
    val score: Int,                           // 综合评分（0-100）
    val level: ScoreLevel,                     // 评分等级
    val dimensions: Map<String, Int>,          // 各维度评分
    val recommendations: List<String>,         // 健康建议
    val statistics: CycleStatistics,           // 统计数据
    val calculatedAt: Long = System.currentTimeMillis() // 计算时间
)
```

#### 2. 评分等级 (ScoreLevel)
```kotlin
/**
 * 评分等级枚举
 */
enum class ScoreLevel(
    val displayName: String,
    val color: String,
    val description: String
) {
    INSUFFICIENT_DATA(
        displayName = "数据不足",
        color = "#999999",
        description = "需要至少3个周期数据才能评分"
    ),
    EXCELLENT(
        displayName = "非常规律",
        color = "#4CAF50",
        description = "您的周期非常规律，健康状况良好"
    ),
    GOOD(
        displayName = "规律",
        color = "#8BC34A",
        description = "您的周期规律，健康状况良好"
    ),
    FAIR(
        displayName = "轻微不规律",
        color = "#FFC107",
        description = "您的周期轻微不规律，建议关注"
    ),
    POOR(
        displayName = "不规律",
        color = "#FF9800",
        description = "您的周期不规律，建议就医检查"
    ),
    VERY_POOR(
        displayName = "非常不规律",
        color = "#F44336",
        description = "您的周期非常不规律，强烈建议就医"
    )
}
```

#### 3. 统计数据 (CycleStatistics)
```kotlin
/**
 * 周期统计数据
 */
data class CycleStatistics(
    val averageCycleLength: Float,             // 平均周期长度（天）
    val cycleStdDev: Float,                    // 周期标准差（天）
    val cycleCoefficientOfVariation: Float,    // 周期变异系数
    val averagePeriodDuration: Float,          // 平均经期天数（天）
    val periodStdDev: Float,                   // 经期标准差（天）
    val abnormalCycleCount: Int,               // 异常周期数量
    val totalCycleCount: Int,                  // 总周期数量
    val trend: CycleTrend                      // 周期趋势
)
```

#### 4. 周期趋势 (CycleTrend)
```kotlin
/**
 * 周期趋势枚举
 */
enum class CycleTrend {
    STABLE,        // 稳定
    IMPROVING,     // 改善
    WORSENING,     // 恶化
    INSUFFICIENT   // 数据不足
}
```

#### 5. 历史评分 (HistoricalScore)
```kotlin
/**
 * 历史评分记录
 */
data class HistoricalScore(
    val date: Long,              // 评分日期（时间戳）
    val score: Int,              // 评分
    val level: ScoreLevel        // 评分等级
)
```

---

## 算法实现

### 评分算法

#### 算法流程
```
1. 数据预处理
   ├─ 排除预测周期
   ├─ 过滤异常数据（周期长度 < 18 或 > 45）
   └─ 选择最近N个周期（默认12个）

2. 数据验证
   └─ 检查数据量是否 >= 3

3. 统计计算
   ├─ 计算平均周期长度
   ├─ 计算周期标准差
   ├─ 计算变异系数（CV = SD / Mean）
   ├─ 计算平均经期天数
   ├─ 计算经期标准差
   └─ 统计异常周期数量

4. 维度评分
   ├─ 周期一致性（40%）
   ├─ 周期正常性（30%）
   ├─ 经期稳定性（20%）
   └─ 数据充足性（10%）

5. 综合评分
   └─ 各维度得分相加

6. 评分等级
   └─ 根据综合评分确定等级

7. 健康建议
   └─ 根据等级和维度得分生成建议
```

#### 维度评分标准

##### 维度1：周期一致性（权重40%）
基于变异系数（CV）评分：
- CV < 0.07: 40分（非常规律）
- CV 0.07-0.15: 30分（规律）
- CV 0.15-0.25: 20分（轻微不规律）
- CV 0.25-0.35: 10分（不规律）
- CV > 0.35: 5分（非常不规律）

##### 维度2：周期正常性（权重30%）
基于平均周期长度评分：
- 25-30天: 30分（非常正常）
- 21-25天 或 30-35天: 25分（正常）
- 18-21天 或 35-45天: 15分（异常）
- < 18天 或 > 45天: 5分（严重异常）

##### 维度3：经期稳定性（权重20%）
基于经期标准差评分：
- SD < 1天: 20分（非常稳定）
- SD 1-2天: 15分（稳定）
- SD 2-3天: 10分（轻微不稳定）
- SD > 3天: 5分（不稳定）

##### 维度4：数据充足性（权重10%）
基于周期数量评分：
- ≥ 12个周期: 10分（数据充足）
- 6-11个周期: 8分（数据良好）
- 3-5个周期: 5分（数据不足）
- < 3个周期: 0分（无法评分）

### 核心类实现

#### RegularityScoreCalculator
```kotlin
/**
 * 周期规律性评分计算器
 */
class RegularityScoreCalculator(
    private val minCycles: Int = 3,
    private val maxCycles: Int = 12
) {

    /**
     * 计算规律性评分
     */
    fun calculate(cycles: List<MenstrualCycle>): RegularityScore {
        // 1. 数据预处理
        val filteredCycles = filterCycles(cycles)
        val recentCycles = selectRecentCycles(filteredCycles)

        // 2. 数据验证
        if (recentCycles.size < minCycles) {
            return createInsufficientDataResult()
        }

        // 3. 计算统计数据
        val statistics = calculateStatistics(recentCycles)

        // 4. 计算各维度评分
        val dimensions = mapOf(
            "consistency" to calculateConsistencyScore(statistics),
            "normality" to calculateNormalityScore(statistics),
            "stability" to calculateStabilityScore(statistics),
            "data" to calculateDataScore(recentCycles.size)
        )

        // 5. 计算综合评分
        val totalScore = dimensions.values.sum()

        // 6. 确定评分等级
        val level = determineScoreLevel(totalScore)

        // 7. 生成建议
        val recommendations = generateRecommendations(
            level,
            totalScore,
            statistics
        )

        return RegularityScore(
            score = totalScore,
            level = level,
            dimensions = dimensions,
            recommendations = recommendations,
            statistics = statistics
        )
    }

    /**
     * 过滤周期数据
     */
    private fun filterCycles(cycles: List<MenstrualCycle>): List<MenstrualCycle> {
        return cycles.filter { cycle ->
            !cycle.isPredicted &&
            cycle.startDate != 0L &&
            cycle.endDate != null &&
            cycle.cycleLength in 18..45
        }
    }

    /**
     * 选择最近的周期
     */
    private fun selectRecentCycles(cycles: List<MenstrualCycle>): List<MenstrualCycle> {
        return cycles
            .sortedByDescending { it.startDate }
            .take(maxCycles)
            .sortedBy { it.startDate }
    }

    /**
     * 计算统计数据
     */
    private fun calculateStatistics(cycles: List<MenstrualCycle>): CycleStatistics {
        val cycleLengths = cycles.map { it.cycleLength.toFloat() }
        val periodDurations = cycles
            .filter { it.periodDuration != null }
            .map { it.periodDuration!!.toFloat() }

        val avgCycleLength = cycleLengths.average().toFloat()
        val avgPeriodDuration = if (periodDurations.isNotEmpty()) {
            periodDurations.average().toFloat()
        } else 0f

        val cycleStdDev = calculateStandardDeviation(cycleLengths, avgCycleLength)
        val periodStdDev = if (periodDurations.isNotEmpty()) {
            calculateStandardDeviation(periodDurations, avgPeriodDuration)
        } else 0f

        val cv = if (avgCycleLength > 0) {
            cycleStdDev / avgCycleLength
        } else 0f

        val abnormalCount = cycles.count {
            it.cycleLength !in 21..35
        }

        val trend = analyzeTrend(cycles)

        return CycleStatistics(
            averageCycleLength = avgCycleLength,
            cycleStdDev = cycleStdDev,
            cycleCoefficientOfVariation = cv,
            averagePeriodDuration = avgPeriodDuration,
            periodStdDev = periodStdDev,
            abnormalCycleCount = abnormalCount,
            totalCycleCount = cycles.size,
            trend = trend
        )
    }

    /**
     * 计算标准差
     */
    private fun calculateStandardDeviation(
        values: List<Float>,
        mean: Float
    ): Float {
        if (values.isEmpty()) return 0f

        val variance = values.map { (it - mean).pow(2) }.average()
        return sqrt(variance).toFloat()
    }

    /**
     * 分析趋势
     */
    private fun analyzeTrend(cycles: List<MenstrualCycle>): CycleTrend {
        if (cycles.size < 3) return CycleTrend.INSUFFICIENT

        val recentScores = cycles.takeLast(3).map { cycle ->
            calculateSingleCycleScore(cycle)
        }

        return when {
            recentScores[2] > recentScores[1] && recentScores[1] > recentScores[0] ->
                CycleTrend.IMPROVING
            recentScores[2] < recentScores[1] && recentScores[1] < recentScores[0] ->
                CycleTrend.WORSENING
            else -> CycleTrend.STABLE
        }
    }

    /**
     * 计算单个周期的评分
     */
    private fun calculateSingleCycleScore(cycle: MenstrualCycle): Float {
        var score = 100f

        // 周期长度评分
        when (cycle.cycleLength) {
            in 25..30 -> score -= 0
            in 21..25, in 30..35 -> score -= 10
            in 18..21, in 35..45 -> score -= 30
            else -> score -= 50
        }

        // 经期天数评分
        cycle.periodDuration?.let { duration ->
            when (duration) {
                in 3..7 -> score -= 0
                in 2..3, in 7..10 -> score -= 10
                else -> score -= 30
            }
        }

        return score
    }

    /**
     * 计算周期一致性评分
     */
    private fun calculateConsistencyScore(statistics: CycleStatistics): Int {
        return when (statistics.cycleCoefficientOfVariation) {
            in 0f..0.07f -> 40
            in 0.07f..0.15f -> 30
            in 0.15f..0.25f -> 20
            in 0.25f..0.35f -> 10
            else -> 5
        }
    }

    /**
     * 计算周期正常性评分
     */
    private fun calculateNormalityScore(statistics: CycleStatistics): Int {
        return when {
            statistics.averageCycleLength in 25f..30f -> 30
            statistics.averageCycleLength in 21f..25f ||
            statistics.averageCycleLength in 30f..35f -> 25
            statistics.averageCycleLength in 18f..21f ||
            statistics.averageCycleLength in 35f..45f -> 15
            else -> 5
        }
    }

    /**
     * 计算经期稳定性评分
     */
    private fun calculateStabilityScore(statistics: CycleStatistics): Int {
        return when (statistics.periodStdDev) {
            in 0f..1f -> 20
            in 1f..2f -> 15
            in 2f..3f -> 10
            else -> 5
        }
    }

    /**
     * 计算数据充足性评分
     */
    private fun calculateDataScore(cycleCount: Int): Int {
        return when (cycleCount) {
            in 12..Int.MAX_VALUE -> 10
            in 6..11 -> 8
            in 3..5 -> 5
            else -> 0
        }
    }

    /**
     * 确定评分等级
     */
    private fun determineScoreLevel(score: Int): ScoreLevel {
        return when (score) {
            in 90..100 -> ScoreLevel.EXCELLENT
            in 80..89 -> ScoreLevel.GOOD
            in 70..79 -> ScoreLevel.FAIR
            in 60..69 -> ScoreLevel.POOR
            else -> ScoreLevel.VERY_POOR
        }
    }

    /**
     * 生成健康建议
     */
    private fun generateRecommendations(
        level: ScoreLevel,
        score: Int,
        statistics: CycleStatistics
    ): List<String> {
        val recommendations = mutableListOf<String>()

        when (level) {
            ScoreLevel.EXCELLENT -> {
                recommendations.add("您的周期非常规律，继续保持健康的生活方式")
            }
            ScoreLevel.GOOD -> {
                recommendations.add("您的周期规律，继续保持良好的生活习惯")
            }
            ScoreLevel.FAIR -> {
                recommendations.add("您的周期轻微不规律，建议注意以下几点：")
                if (statistics.cycleCoefficientOfVariation > 0.15) {
                    recommendations.add("- 保持规律的作息时间")
                    recommendations.add("- 减少压力，适当运动")
                }
                if (statistics.averagePeriodDuration !in 3f..7f) {
                    recommendations.add("- 注意经期护理，如异常请就医")
                }
            }
            ScoreLevel.POOR -> {
                recommendations.add("您的周期不规律，建议：")
                recommendations.add("- 记录详细的生活习惯（饮食、运动、睡眠）")
                recommendations.add("- 避免过度节食或暴饮暴食")
                recommendations.add("- 如持续不规律，建议就医检查")
                if (statistics.abnormalCycleCount > 0) {
                    recommendations.add("- 您的周期长度超出正常范围，请重点关注")
                }
            }
            ScoreLevel.VERY_POOR -> {
                recommendations.add("您的周期非常不规律，强烈建议：")
                recommendations.add("- 尽快就医检查内分泌功能")
                recommendations.add("- 可能的疾病：多囊卵巢综合征、甲状腺功能异常等")
                recommendations.add("- 记录详细的症状和生活日志，帮助医生诊断")
            }
            ScoreLevel.INSUFFICIENT_DATA -> {
                recommendations.add("请继续记录至少3个周期数据以获得准确评分")
            }
        }

        return recommendations
    }

    /**
     * 创建数据不足结果
     */
    private fun createInsufficientDataResult(): RegularityScore {
        return RegularityScore(
            score = 0,
            level = ScoreLevel.INSUFFICIENT_DATA,
            dimensions = emptyMap(),
            recommendations = listOf("请继续记录至少3个周期数据以获得准确评分"),
            statistics = CycleStatistics(
                averageCycleLength = 0f,
                cycleStdDev = 0f,
                cycleCoefficientOfVariation = 0f,
                averagePeriodDuration = 0f,
                periodStdDev = 0f,
                abnormalCycleCount = 0,
                totalCycleCount = 0,
                trend = CycleTrend.INSUFFICIENT
            )
        )
    }
}
```

---

## 界面设计

### UI组件设计

#### 1. 评分总览卡片
```
┌─────────────────────────────────────┐
│  ┌───────┐                          │
│  │  85   │  规律                    │
│  │       │  综合评分：85分          │
│  │  🟢   │  您的周期规律，健康状况良好│
│  └───────┘                          │
└─────────────────────────────────────┘
```

#### 2. 维度评分卡片
```
┌─────────────────────────────────────┐
│ 各维度评分                          │
│                                     │
│ 周期一致性  ████████████░░░░░░ 32/40│
│ 周期正常性  ████████████░░░░░░ 25/30│
│ 经期稳定性  ████████████░░░░░░ 18/20│
│ 数据充足性  ████████░░░░░░░░░░░  8/10│
└─────────────────────────────────────┘
```

#### 3. 统计数据卡片
```
┌─────────────────────────────────────┐
│ 统计数据                            │
│                                     │
│ 平均周期长度     28.5天            │
│ 周期标准差       1.2天             │
│ 变异系数         4.2%              │
│ 平均经期天数     5.3天             │
│ 经期标准差       0.8天             │
│ 异常周期数量     0/6               │
└─────────────────────────────────────┘
```

#### 4. 健康建议卡片
```
┌─────────────────────────────────────┐
│ 健康建议                            │
│                                     │
│ ✓ 您的周期规律，继续保持良好的生活习惯│
│                                     │
│ • 保持规律的作息时间                │
│ • 适量运动，增强体质                │
│ • 注意饮食均衡                      │
└─────────────────────────────────────┘
```

#### 5. 历史趋势图表
```
┌─────────────────────────────────────┐
│ 历史趋势（近12个月）                │
│                                     │
│ 评分 │         ┌─────┐             │
│  100 │      ┌──┘     └──┐          │
│   80 │  ┌───┘            └─┐       │
│   60 │──┘                  └───┐   │
│   40 │                         └───│
│   20 │                             │
│   0  └──────────────────────────── │
│      1月 3月 5月 7月 9月 11月      │
└─────────────────────────────────────┘
```

### Compose UI实现

#### ScoreScreen
```kotlin
@Composable
fun ScoreScreen(
    viewModel: ScoreViewModel = hiltViewModel()
) {
    val scoreState by viewModel.scoreState.collectAsState()
    val isLoading by viewModel.isLoading.collectAsState()
    val error by viewModel.error.collectAsState()

    LaunchedEffect(Unit) {
        viewModel.loadCurrentScore()
        viewModel.loadHistoricalScores()
    }

    Scaffold(
        topBar = {
            TopAppBar(
                title = { Text("周期稳定性") },
                actions = {
                    IconButton(onClick = { viewModel.refreshScore() }) {
                        Icon(Icons.Default.Refresh, "刷新")
                    }
                }
            )
        }
    ) { padding ->
        when {
            isLoading -> {
                Box(
                    modifier = Modifier
                        .fillMaxSize()
                        .padding(padding),
                    contentAlignment = Alignment.Center
                ) {
                    CircularProgressIndicator()
                }
            }
            error != null -> {
                Box(
                    modifier = Modifier
                        .fillMaxSize()
                        .padding(padding),
                    contentAlignment = Alignment.Center
                ) {
                    Text(
                        text = error ?: "加载失败",
                        color = MaterialTheme.colorScheme.error
                    )
                }
            }
            scoreState != null -> {
                LazyColumn(
                    modifier = Modifier
                        .fillMaxSize()
                        .padding(padding),
                    contentPadding = PaddingValues(16.dp),
                    verticalArrangement = Arrangement.spacedBy(16.dp)
                ) {
                    item {
                        ScoreOverviewCard(scoreState!!)
                    }

                    item {
                        DimensionScoresCard(scoreState!!)
                    }

                    item {
                        StatisticsCard(scoreState!!)
                    }

                    item {
                        RecommendationsCard(scoreState!!)
                    }

                    item {
                        HistoricalTrendCard(viewModel.historicalScores)
                    }
                }
            }
        }
    }
}
```

#### ScoreOverviewCard
```kotlin
@Composable
fun ScoreOverviewCard(score: RegularityScore) {
    Card(
        modifier = Modifier.fillMaxWidth(),
        elevation = CardDefaults.cardElevation(defaultElevation = 4.dp)
    ) {
        Row(
            modifier = Modifier
                .fillMaxWidth()
                .padding(20.dp),
            horizontalArrangement = Arrangement.spacedBy(20.dp),
            verticalAlignment = Alignment.CenterVertically
        ) {
            // 评分环形图
            ScoreCircleView(
                score = score.score,
                level = score.level,
                modifier = Modifier.size(120.dp)
            )

            // 评分信息
            Column {
                Text(
                    text = score.level.displayName,
                    style = MaterialTheme.typography.headlineMedium,
                    color = Color.parseColor(score.level.color)
                )
                Text(
                    text = "综合评分：${score.score}分",
                    style = MaterialTheme.typography.titleMedium,
                    modifier = Modifier.padding(top = 8.dp)
                )
                Text(
                    text = score.level.description,
                    style = MaterialTheme.typography.bodyMedium,
                    color = MaterialTheme.colorScheme.onSurfaceVariant,
                    modifier = Modifier.padding(top = 4.dp)
                )
            }
        }
    }
}
```

---

## 开发计划

### Phase 1: 数据层开发（第1周）
- [ ] 定义数据模型（RegularityScore, ScoreLevel, CycleStatistics）
- [ ] 扩展MenstrualCycleDao接口
- [ ] 实现RegularityScoreRepository
- [ ] 编写数据层单元测试

### Phase 2: 领域层开发（第2周）
- [ ] 实现RegularityScoreCalculator核心算法
- [ ] 实现各维度评分计算
- [ ] 实现趋势分析算法
- [ ] 实现健康建议生成逻辑
- [ ] 编写算法单元测试

### Phase 3: 业务层开发（第3周）
- [ ] 实现CalculateRegularityScoreUseCase
- [ ] 实现GetScoreTrendUseCase
- [ ] 实现ScoreViewModel
- [ ] 实现StateFlow数据流

### Phase 4: UI层开发（第4-5周）
- [ ] 实现ScoreScreen主界面
- [ ] 实现ScoreCircleView评分环形图
- [ ] 实现DimensionProgressView维度进度条
- [ ] 实现TrendChartView趋势图表
- [ ] 实现各个卡片组件

### Phase 5: 集成与优化（第6周）
- [ ] 集成导航
- [ ] 实现自动计算触发逻辑
- [ ] 性能优化
- [ ] UI动画效果
- [ ] 深色模式适配

### Phase 6: 测试与修复（第7周）
- [ ] 端到端测试
- [ ] 边界情况测试
- [ ] 性能测试
- [ ] Bug修复
- [ ] 代码审查

---

## 测试方案

### 单元测试

#### 算法测试
```kotlin
class RegularityScoreCalculatorTest {

    private val calculator = RegularityScoreCalculator()

    @Test
    fun `calculate should return insufficient data when cycles less than 3`() {
        val cycles = listOf(
            createCycle(cycleLength = 28),
            createCycle(cycleLength = 29)
        )

        val result = calculator.calculate(cycles)

        assertEquals(0, result.score)
        assertEquals(ScoreLevel.INSUFFICIENT_DATA, result.level)
    }

    @Test
    fun `calculate should return excellent score for regular cycles`() {
        val cycles = listOf(
            createCycle(cycleLength = 28, periodDuration = 5),
            createCycle(cycleLength = 29, periodDuration = 5),
            createCycle(cycleLength = 28, periodDuration = 5),
            createCycle(cycleLength = 27, periodDuration = 6),
            createCycle(cycleLength = 28, periodDuration = 5),
            createCycle(cycleLength = 29, periodDuration = 5)
        )

        val result = calculator.calculate(cycles)

        assertTrue(result.score >= 80)
        assertTrue(result.level in listOf(ScoreLevel.EXCELLENT, ScoreLevel.GOOD))
    }

    @Test
    fun `calculate should return poor score for irregular cycles`() {
        val cycles = listOf(
            createCycle(cycleLength = 20, periodDuration = 3),
            createCycle(cycleLength = 40, periodDuration = 10),
            createCycle(cycleLength = 18, periodDuration = 2),
            createCycle(cycleLength = 45, periodDuration = 12)
        )

        val result = calculator.calculate(cycles)

        assertTrue(result.score < 70)
        assertTrue(result.level in listOf(ScoreLevel.POOR, ScoreLevel.VERY_POOR))
    }

    @Test
    fun `calculate should filter out predicted cycles`() {
        val cycles = listOf(
            createCycle(cycleLength = 28, isPredicted = false),
            createCycle(cycleLength = 29, isPredicted = false),
            createCycle(cycleLength = 28, isPredicted = true), // 预测周期
            createCycle(cycleLength = 27, isPredicted = false)
        )

        val result = calculator.calculate(cycles)

        // 应该只使用3个非预测周期
        assertEquals(3, result.statistics.totalCycleCount)
    }

    @Test
    fun `calculate should filter out abnormal cycles`() {
        val cycles = listOf(
            createCycle(cycleLength = 28),
            createCycle(cycleLength = 29),
            createCycle(cycleLength = 50), // 异常周期（>45）
            createCycle(cycleLength = 10)  // 异常周期（<18）
        )

        val result = calculator.calculate(cycles)

        // 应该只使用2个正常周期
        assertEquals(2, result.statistics.totalCycleCount)
        assertEquals(ScoreLevel.INSUFFICIENT_DATA, result.level)
    }
}
```

#### Repository测试
```kotlin
class RegularityScoreRepositoryTest {

    @get:Rule
    val instantTaskExecutorRule = InstantTaskExecutorRule()

    private lateinit var database: WomenHealthDatabase
    private lateinit var dao: MenstrualCycleDao
    private lateinit var repository: RegularityScoreRepository

    @Before
    fun setup() {
        database = Room.inMemoryDatabaseBuilder(
            ApplicationProvider.getApplicationContext(),
            WomenHealthDatabase::class.java
        ).build()

        dao = database.menstrualCycleDao()
        repository = RegularityScoreRepository(dao)
    }

    @After
    fun teardown() {
        database.close()
    }

    @Test
    fun `calculateCurrentScore should return score with valid data`() = runTest {
        // 插入测试数据
        val cycles = (0..5).map { i ->
            MenstrualCycle(
                id = i.toLong(),
                startDate = System.currentTimeMillis() - (i * 30 * 24 * 60 * 60 * 1000L),
                cycleLength = 28,
                periodDuration = 5
            )
        }
        dao.insertAll(cycles)

        val result = repository.calculateCurrentScore()

        assertNotNull(result)
        assertTrue(result.score > 0)
    }
}
```

### 集成测试

```kotlin
@HiltAndroidTest
class ScoreScreenTest {

    @get:Rule
    val hiltRule = HiltAndroidRule(this)

    @get:Rule
    val composeTestRule = createComposeRule()

    @Inject
    lateinit var repository: RegularityScoreRepository

    @Before
    fun setup() {
        hiltRule.inject()
    }

    @Test
    fun `scoreScreen should display score overview`() {
        composeTestRule.setContent {
            ScoreScreen()
        }

        // 等待数据加载
        composeTestRule.waitUntil(5000) {
            composeTestRule.onNodeWithText("综合评分").isDisplayed()
        }

        // 验证评分显示
        composeTestRule.onNodeWithText("综合评分").assertIsDisplayed()
    }

    @Test
    fun `scoreScreen should display dimension scores`() {
        composeTestRule.setContent {
            ScoreScreen()
        }

        // 等待数据加载
        composeTestRule.waitUntil(5000) {
            composeTestRule.onNodeWithText("各维度评分").isDisplayed()
        }

        // 验证维度评分显示
        composeTestRule.onNodeWithText("周期一致性").assertIsDisplayed()
        composeTestRule.onNodeWithText("周期正常性").assertIsDisplayed()
        composeTestRule.onNodeWithText("经期稳定性").assertIsDisplayed()
        composeTestRule.onNodeWithText("数据充足性").assertIsDisplayed()
    }
}
```

---

## 风险提示

### 技术风险

#### 1. 数据质量问题
- **风险**: 用户记录的数据可能不完整或不准确
- **影响**: 评分结果可能不准确
- **缓解措施**:
  - 实现数据验证和过滤
  - 提供数据质量提示
  - 使用异常值检测算法

#### 2. 算法准确性
- **风险**: 评分算法可能不符合医学标准
- **影响**: 误导用户，影响健康决策
- **缓解措施**:
  - 参考医学文献和标准
  - 咨询专业医生
  - 明确标注仅供参考

#### 3. 性能问题
- **风险**: 大量数据计算可能导致性能下降
- **影响**: 用户体验差
- **缓解措施**:
  - 限制计算数据量
  - 使用缓存策略
  - 异步计算

### 业务风险

#### 1. 误诊风险
- **风险**: 用户可能过度依赖评分结果
- **影响**: 延误就医或错误判断
- **缓解措施**:
  - 明确免责声明
  - 提供就医建议
  - 不替代专业医疗诊断

#### 2. 隐私风险
- **风险**: 用户健康数据泄露
- **影响**: 用户隐私泄露
- **缓解措施**:
  - 本地加密存储
  - 不上传云端
  - 提供数据删除功能

### 合规风险

#### 1. 医疗器械法规
- **风险**: 可能被认定为医疗器械
- **影响**: 需要医疗器械注册
- **缓解措施**:
  - 明确标注非医疗器械
  - 不提供诊断功能
  - 仅提供健康参考

#### 2. 数据保护法规
- **风险**: 违反GDPR、网络安全法等法规
- **影响**: 法律责任
- **缓解措施**:
  - 符合数据保护法规
  - 提供隐私政策
  - 用户数据控制权

---

## 附录

### A. 参考文档
- CYCLE_REGULARITY_SCORING_SPEC.md - 评分算法详细规范
- TECHNICAL_SPEC.md - 技术架构文档
- WOMEN_HEALTH_APP_DEV_SPEC.md - 应用开发规范

### B. 医学参考
- 正常月经周期：21-35天
- 正常经期：2-7天
- 变异系数：正常<15%

### C. 术语表
| 术语 | 说明 |
|------|------|
| CV | 变异系数（Coefficient of Variation）|
| SD | 标准差（Standard Deviation）|
| LMP | 末次月经（Last Menstrual Period）|

### D. 更新日志
| 版本 | 日期 | 更新内容 |
|------|------|---------|
| v1.0 | 2026-02-14 | 初始版本 |

---

**文档维护**: 开发团队
**最后更新**: 2026-02-14
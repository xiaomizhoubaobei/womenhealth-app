# Secrets Gradle Plugin default properties file.
# This file should contain placeholder values only.
# Do NOT define GEMINI_API_KEY here — it is injected via
# buildConfigField in build.gradle.kts to avoid Secrets Gradle Plugin
# generating illegal Java expressions for empty values.
